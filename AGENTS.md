# Cyber Kingdom 開發規範

## 產品方向

- Godot / GDScript，iPhone 與 iPad 優先，橫向捲軸像素動作 Roguelite。
- 正式平台優先順序是 macOS／Windows 桌面版與 iPhone／iPad App；H5 只作快速試玩、回饋與部署驗收入口。效能、記憶體、輸入手感與畫面品質先以原生桌面／iOS 建置量測，除非不影響原生版本，不為 WebAssembly 加入專屬架構或玩法折衷。
- 人類靠龍晶驅動機械義肢，在魔法與巨龍統治的世界建立最後的避難所。玩家每次出征都要權衡強化自己或保護居民；完整準則見 `docs/design/vision.md`。
- 騎士負責探索、標記工作、投入建設與戰鬥；砍樹、開採、採果、採藥和施工由居民執行；寶箱由騎士直接開啟。工匠搬運入庫，清理後可建拓荒站擴展收貨範圍。
- 目前戰役只使用龍晶作為採集收益和付款資源；樹木、礦脈、農田與狩獵保留差異，收益留在場景供騎士收取，不恢復木材／食物等共享庫存。
- 啟動先顯示起始頁，可建立獨立旅程或選擇既有紀錄；起始頁允許必要文字按鈕。
- 目前主場景從荒地步行至營火起家，騎士背包有限；新旅程拔劍點火後建國；現場每次投入一顆龍晶，未填滿而停止投入後保留一秒，可單點續填並重計一秒，逾時退回建築下方。夜襲逐日變強，日夜規格見 `docs/design/campfire-campaign.md`。
- 封印左右兩門召喚最終巨龍；擊敗巨龍且清除殘敵才通關。強度最低第 6 天，較晚召喚逐日成長，召喚後鎖定。見 `docs/design/dragon-frontier.md`。
- 避難所採現場投入、居民自主取器具與工作；居民只在實際受擊且未被防護擋住時退回流浪者，細則見 `docs/design/walkable-refuge.md`。
- 戰鬥與避難所經營先分開驗證；主戰役改為平地探索與兩段快慢移動，不需要跳躍或瞬間衝刺。
- 騎士不按方向的三段連斬全部原地；按住方向才使用獨立踏斬動作並在有效揮擊段向前移動。放開即停止推進、不補發未用位移；每刀保持已鎖定面向，方向輸入可影響下一刀。不要恢復無輸入的自動連斬踏進。
- 使用者一人製作，以低固定成本、可維護與市場驗證為優先。
- 美術像素密度一致：角色、建築與場景先製成共同遊戲像素網格的素材，再放大顯示；不得直接把高解析原稿縮放當正式圖素。動作全循環使用固定比例，預覽與接入資源須同源；規格與舊素材差異見 `docs/design/pixel-density.md`。
- 新增或修改玩家文字須同步繁中、簡中、英文目錄，保留格式占位符；不要把翻譯文字當作玩法判斷值。
- 新旅程移除常駐數值 HUD，以袋子、天色與動作回饋呈現；日出短暫顯示天數。主玩法介面使用圖示與必要數字；不要恢復常駐文字面板、文字按鈕或人物／建築文字牌。成本與材料圖示必須讀實際互動規則。
- 最新感受式旅程採龍晶生存：容量最多 30，受擊先掉晶、空袋下一擊掉劍、空袋無劍再受擊死亡；一般小怪抱一顆晶撤回原側門。晶袋收支才短暫顯示、可見入袋出袋，禁止恢復常駐血量數字。規格見 `docs/design/crystal-survival.md`。
- 與使用者以繁體中文溝通，每個開發單元附一段對應到實際專案的 Godot 教學。

- 新旅程保留獵人／弓兵，三級營火解鎖槍盾兵；射箭場只供器具，不升級。移除能量護盾。居民夜間照常工作，附近有敵人才避難。以 `docs/design/immersive-kingdom-loop.md` 為新規則；舊存檔保留原規則，舊規格見 `docs/design/flat-frontier.md`。

## 架構

- `domain` 不依賴外層，`application` 只依賴 domain 與自己的 ports。
- 不把 Input、Node、FileAccess 或 UI 操作放進 domain/application。
- Godot 節點放 presentation，存檔等外部操作放 infrastructure，由 bootstrap 組裝。
- 數值以 Resource 提供 Inspector 編輯；不要替尚未存在的需求製造框架。
- 外層可呼叫內層；內層透過 port 表達外部需求。

## TDD 與驗證

- 新遊戲規則與 bug 修正先寫有行為意義的失敗測試，確認失敗原因，再實作、重構。
- 不用只驗證常數或複製實作的測試充數；畫面與手感須另行實際驗證。
- 交付前執行 `bash tools/check.sh`，檢查 Godot SCRIPT ERROR，不能只信程序退出碼。
- 變更儲存格式需保護未知版本和損壞資料，不可靜默覆蓋。

## Gitflow

- `main` 放正式版本；`develop` 整合下一版本。
- 功能從 develop 建 `feature/<name>`；完成驗證再以 `--no-ff` 合併 develop。
- 發行使用 `release/<version>`；正式修補使用 `hotfix/<name>`，都須回合 develop。
- 不強制推送、不重寫既有歷史；不要提交憑證、引擎快取或測試存檔。
- 提交要小且說明目的；更新 docs/STATUS.md，記錄測試和下一步。

## 專案知識入口

- 開始功能設計、實作、除錯或審查時，先讀 `CONTEXT.md`、`docs/agents/mattpocock-adoption.md` 與相關 ADR；同一工作單元已讀且未變更時不必重讀。
- 撰寫或審查程式前讀 `docs/agents/gdscript-standards.md`（分層、型別、tick、亂數、內容資料、modifier、存檔、測試的底線，以及各條規則的現況落差與落地階段）。
- 本地 Matt Pocock 技能包作為按需參考；適配與來源版本以 adoption 文件為準。保留現有 Godot、Clean Architecture、TDD、Gitflow 及使用者授權。
- 遊戲術語集中在 `CONTEXT.md`，進度集中在 `docs/STATUS.md`。
- 教學前讀 `docs/lessons/learning-records/`，以使用者實際回饋更新掌握程度。
