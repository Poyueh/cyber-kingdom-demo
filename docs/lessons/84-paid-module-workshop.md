# 第 84 課：玩法費用與畫面分開

這次的部件工坊由三個部分合作：`application/knight_modules.gd` 管理已取得的部件與冷卻；`application/campaign_session.gd` 檢查是否站在工坊、龍晶是否足夠，再扣款換裝；`presentation/module_menu.gd` 顯示可選項目與價格。即使畫面按鈕被意外觸發，玩法層仍會拒絕未取得、重複選擇或付不起的換裝。

在 Godot 開啟 `data/campaign.tres`，Inspector 找到 `Crystal Prices` 字典中的 `module_swap`，目前為 8。調整後開始新旅程，即可測試其他費用；既有存檔保留建立旅程時的設定。選單與扣款都讀同一份部件設定，不必各改一次。

本次先寫拾取即裝備、8 顆換裝、禁止升級與存檔冷卻的測試，再完成實作。冷卻完成只送出一次效果事件，由畫面層畫出短暫光環，玩法層不建立特效節點。

試玩：探索取得兩個部件 → 回營火旁工坊按 F（手機點齒輪）→ 選另一個部件 → 確認少 8 顆龍晶。K／手機雙指上滑使用能力；冷卻完成會亮起光環。
